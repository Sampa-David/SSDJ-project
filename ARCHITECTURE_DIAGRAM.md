# 🏗️ System Architecture Diagram

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        CLIENT BROWSER (Web)                         │
│  User Interface with Bootstrap 5 | Responsive Design | Blade Views  │
└────────────────────────────────┬──────────────────────────────────────┘
                                 │
                    HTTP Requests / Responses
                                 │
┌────────────────────────────────▼──────────────────────────────────────┐
│                        Laravel 11 Application                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              Routes (routes/web.php)                         │   │
│  │  Public Routes | Auth Routes | Protected Ticket Routes      │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                              │                                       │
│  ┌───────────────────────────▼──────────────────────────────────┐   │
│  │             Controllers (HTTP Logic Layer)                   │   │
│  │  ┌─────────────────────────┬─────────────────────────────┐  │   │
│  │  │   AuthController        │   TicketController          │  │   │
│  │  │  • showRegister()       │  • showPurchase()           │  │   │
│  │  │  • register()           │  • purchase()               │  │   │
│  │  │  • showLogin()          │  • myTickets()              │  │   │
│  │  │  • login()              │  • show()                   │  │   │
│  │  │  • logout()             │  • cancel()                 │  │   │
│  │  │                         │  • download()               │  │   │
│  │  │                         │  • confirmation()           │  │   │
│  │  │                         │  • dashboard()              │  │   │
│  │  └─────────────────────────┴─────────────────────────────┘  │   │
│  └──────────────┬──────────────────────────────┬────────────────┘   │
│                 │                              │                    │
│  ┌──────────────▼────────────┐   ┌────────────▼──────────────┐     │
│  │   Models (Eloquent ORM)   │   │   Views (Blade Templates) │     │
│  │  ┌──────────────────────┐ │   │  ┌──────────────────────┐│     │
│  │  │   User Model         │ │   │  │  auth/register      ││     │
│  │  │ • tickets()          │ │   │  │  auth/login         ││     │
│  │  │ • activeTickets()    │ │   │  │  dashboard          ││     │
│  │  │ • getTotalSpent()    │ │   │  │  tickets/purchase   ││     │
│  │  │ • getTicketCountBy   │ │   │  │  tickets/my-tickets ││     │
│  │  │   Type()             │ │   │  │  tickets/show       ││     │
│  │  └──────────────────────┘ │   │  │  tickets/confirm    ││     │
│  │  ┌──────────────────────┐ │   │  └──────────────────────┘│     │
│  │  │   Ticket Model       │ │   │  Bootstrap 5 Styling      │     │
│  │  │ • belongsTo(User)    │ │   │  Gradient Theme            │     │
│  │  │ • isValid()          │ │   │  Responsive Grid           │     │
│  │  │ • generateTicket     │ │   │  Color-coded Badges        │     │
│  │  │   Number()           │ │   │  Emoji Icons               │     │
│  │  │ • getTypeLabel()     │ │   │                            │     │
│  │  │ • getPriceDisplay()  │ │   │                            │     │
│  │  └──────────────────────┘ │   │                            │     │
│  └──────────────┬─────────────┘   └────────────┬──────────────┘     │
│                 │                              │                    │
└─────────────────┼──────────────────────────────┼────────────────────┘
                  │                              │
                  │          Database            │
                  │       (MySQL 8.4.7)          │
                  │                              │
┌─────────────────▼──────────────────────────────▼────────────────────┐
│                    Database Layer                                   │
│  ┌───────────────────────────┬──────────────────────────────────┐   │
│  │    Users Table            │    Tickets Table               │   │
│  │                           │                                 │   │
│  │  • id (PK)                │  • id (PK)                     │   │
│  │  • name                   │  • user_id (FK)                │   │
│  │  • email (UNIQUE)         │  • ticket_type                 │   │
│  │  • password (bcrypt)      │  • price                       │   │
│  │  • phone                  │  • ticket_number (UNIQUE)      │   │
│  │  • company                │  • status                      │   │
│  │  • timestamps             │  • purchased_at                │   │
│  │                           │  • valid_from                  │   │
│  │                           │  • valid_until                 │   │
│  │  Relationship:            │  • qr_code                     │   │
│  │  One-to-Many              │  • notes                       │   │
│  │  1 User → Many Tickets    │  • timestamps                  │   │
│  │                           │  • Cascade Delete on User      │   │
│  └───────────────────────────┴──────────────────────────────────┘   │
│                                                                      │
│  Indexes: user_id, status | email (UNIQUE)                         │
│  Charset: utf8mb4 | Engine: InnoDB                                 │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Request-Response Flow

### Registration Flow
```
┌─────────────────────────────────────────────────────────────┐
│ 1. User navigates to /register                              │
│    GET /register                                             │
│    → AuthController::showRegister()                         │
│    ← return view('auth.register')                           │
│    → Display: Registration form                             │
└─────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. User submits registration form                           │
│    POST /register                                            │
│    Data: name, email, password, phone, company             │
│    → AuthController::register()                            │
│    ├─ Validate input (name, email, password, confirm)     │
│    ├─ Hash password with bcrypt                           │
│    ├─ Create User record in database                      │
│    └─ Return success message                              │
│    ← redirect('/login')                                    │
│    → Display: Login form with success message              │
└─────────────────────────────────────────────────────────────┘
```

### Authentication Flow
```
┌─────────────────────────────────────────────────────────────┐
│ 1. User navigates to /login                                 │
│    GET /login                                               │
│    → AuthController::showLogin()                           │
│    ← return view('auth.login')                             │
│    → Display: Login form                                    │
└─────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. User submits login form                                  │
│    POST /login                                              │
│    Data: email, password, remember_me                      │
│    → AuthController::login()                               │
│    ├─ Validate email & password                           │
│    ├─ Attempt authentication                              │
│    ├─ If valid:                                           │
│    │  ├─ Regenerate session ID                           │
│    │  ├─ Set session timeout (remember_me option)        │
│    │  └─ Redirect to /dashboard                         │
│    └─ If invalid:                                         │
│       └─ Return with error message                        │
│    ← Session cookie created + stored in browser            │
│    → Display: User dashboard                              │
└─────────────────────────────────────────────────────────────┘
```

### Ticket Purchase Flow
```
┌─────────────────────────────────────────────────────────────┐
│ 1. Authenticated user clicks "Buy Tickets"                  │
│    GET /buy-tickets                                         │
│    → TicketController::showPurchase()                      │
│    ├─ Prepare ticketTypes array (3 tiers)                 │
│    └─ return view('tickets.purchase', compact('tTypes'))  │
│    → Display: Purchase page with 3 options                │
└─────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. User selects ticket type and quantity                    │
│    POST /tickets/purchase                                   │
│    Data: ticket_type, quantity                             │
│    → TicketController::purchase()                          │
│    ├─ Validate ticket_type in ['early_bird', 'regular',   │
│    │  'premium']                                           │
│    ├─ Validate quantity in [1..10]                        │
│    ├─ For each ticket (1 to quantity):                    │
│    │  ├─ Generate unique ticket_number (TKT-XXXXXXXX)    │
│    │  ├─ Generate QR code (base64 encoded)               │
│    │  └─ Create Ticket record with:                      │
│    │     ├─ user_id = Auth::id()                         │
│    │     ├─ ticket_type, price, ticket_number           │
│    │     ├─ status = 'active'                           │
│    │     ├─ purchased_at = now()                        │
│    │     ├─ valid_from/until dates                      │
│    │     └─ qr_code                                     │
│    └─ Redirect to confirmation                           │
│    ← DB: Create 1-10 Ticket rows                         │
│    → Display: Confirmation page                           │
└─────────────────────────────────────────────────────────────┘
```

### Dashboard Flow
```
┌─────────────────────────────────────────────────────────────┐
│ 1. Authenticated user navigates to /dashboard               │
│    GET /dashboard (auth middleware required)               │
│    → TicketController::dashboard()                         │
│    ├─ Retrieve user (Auth::user())                        │
│    ├─ Load tickets paginated (10 per page):              │
│    │  └─ Auth::user()->tickets()->latest()->paginate(10)  │
│    ├─ Calculate statistics:                               │
│    │  ├─ total = Auth::user()->tickets()->count()        │
│    │  ├─ active = Auth::user()->activeTickets()->count() │
│    │  ├─ cancelled = WHERE status = 'cancelled'          │
│    │  └─ total_spent = getTotalSpentAttribute()          │
│    ├─ Prepare $stats array                               │
│    └─ return view('dashboard', compact('tickets', 'stats'))
│    ← DB: ~4-5 queries (users, tickets count, pagination) │
│    → Display: Dashboard with stats & tickets              │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Model Relationships

```
User
├─── id (PK)
├─── name
├─── email (UNIQUE)
├─── password (bcrypted)
├─── phone
├─── company
└─── timestamps

     │
     │ (1:Many)
     │ HasMany relationship
     │
     ▼

Ticket
├─── id (PK)
├─── user_id (FK → User.id) [UNIQUE INDEX]
├─── ticket_type ENUM('early_bird', 'regular', 'premium')
├─── price DECIMAL(8,2)
├─── ticket_number VARCHAR(UNIQUE) [TKT-XXXXXXXXXX format]
├─── status ENUM('active', 'cancelled', 'used') [INDEX]
├─── purchased_at DATETIME
├─── valid_from DATETIME
├─── valid_until DATETIME
├─── qr_code LONGTEXT
├─── notes TEXT
└─── timestamps (created_at, updated_at)
```

---

## Middleware & Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    Request Processing                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Global Middleware                                       │
│     ├─ EncryptCookies                                      │
│     ├─ AddQueuedCookiesToResponse                          │
│     ├─ StartSession                                        │
│     ├─ AuthenticateSession                                 │
│     ├─ VerifyCsrfToken                    ← @csrf in forms│
│     └─ PreventRequestsDuringMaintenance                   │
│                                                              │
│  2. Route Group Middleware                                  │
│     ├─ auth (for /dashboard, /tickets/*, etc)            │
│     └─ guest (for /register, /login)                      │
│                                                              │
│  3. Method-Level Authorization                             │
│     └─ Authorization::check($ticket->user_id === Auth::id())
│                                                              │
│  4. Database Layer                                          │
│     ├─ Foreign Key Constraints                            │
│     ├─ Cascade Delete                                     │
│     └─ Unique Constraints                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Authentication States

```
┌─────────────────────────────────────────────────────────┐
│             Authentication State Diagram                 │
└─────────────────────────────────────────────────────────┘

        ┌─────────────┐
        │  NOT LOGGED │
        │             │
        └──────┬──────┘
               │
    ┌──────────┴──────────┐
    │                     │
    ▼                     ▼
┌──────────┐          ┌─────────┐
│ Register │          │  Login  │
└────┬─────┘          └────┬────┘
     │                     │
     └──────────┬──────────┘
                │ (Success)
                ▼
        ┌──────────────┐
        │   LOGGED IN  │  ← Session active
        │              │    LARAVEL_SESSION cookie
        └────┬─────┬──┘
             │     │
             │     └─→ Access /dashboard, /tickets/*
             │
             │ (Logout)
             │
             ▼
        ┌──────────────┐
        │  NOT LOGGED  │  ← Session destroyed
        │              │    Cookie cleared
        └──────────────┘
```

---

## API Routes Overview

```
PUBLIC ROUTES
  GET  /                              → Homepage
  GET  /schedule                      → Schedule page
  GET  /speakers                      → Speakers page
  GET  /venue                         → Venue info
  GET  /terms                         → Terms
  GET  /privacy                       → Privacy
  GET  /contact                       → Contact

AUTH ROUTES (Guest Only)
  GET  /register                      → Show registration form
  POST /register                      → Process registration
  GET  /login                         → Show login form
  POST /login                         → Process login

PROTECTED ROUTES (Auth Required)
  POST /logout                        → Logout user
  GET  /dashboard                     → User dashboard
  GET  /buy-tickets                   → Show purchase form
  POST /tickets/purchase              → Create ticket purchase
  GET  /my-tickets                    → List user's tickets
  GET  /tickets/{ticket}              → Show ticket details
  DELETE /tickets/{ticket}/cancel     → Cancel ticket
  GET  /tickets/{ticket}/download     → Download ticket
  GET  /tickets/confirmation/{ticket} → Purchase confirmation
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────┐
│              Performance Considerations                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│ 1. Database Queries                                         │
│    ├─ Indexed Fields:                                      │
│    │  ├─ users.email (UNIQUE)                             │
│    │  ├─ tickets.user_id (Foreign Key)                    │
│    │  └─ tickets.status (Status filtering)                │
│    │                                                        │
│    ├─ Eager Loading:                                       │
│    │  ├─ Use with() for related data                      │
│    │  └─ Prevent N+1 queries                              │
│    │                                                        │
│    └─ Pagination:                                          │
│       ├─ Dashboard: 10 tickets per page                   │
│       └─ My Tickets: 10 tickets per page                  │
│                                                              │
│ 2. Caching                                                  │
│    ├─ Query Caching (optional)                            │
│    └─ View Caching (production)                           │
│       └─ php artisan view:cache                           │
│                                                              │
│ 3. Asset Optimization                                       │
│    ├─ CSS: Bootstrap CDN                                  │
│    ├─ JS: Bootstrap JS                                    │
│    └─ Minification: npm run build                         │
│                                                              │
│ 4. Response Time Targets                                   │
│    ├─ Homepage: < 2s                                      │
│    ├─ Dashboard: < 1.5s                                   │
│    ├─ Purchase: < 1s                                      │
│    └─ DB Queries: < 100ms                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌──────────────────────────────────┐
│      Exception Occurs            │
└────────┬─────────────────────────┘
         │
    ┌────▼─────────────────────────────────┐
    │  Exception Type?                     │
    └────┬───────────────────────┬─────────┘
         │                       │
         ▼                       ▼
    ┌─────────────┐          ┌────────────────┐
    │ 404         │          │ 403            │
    │ Not Found   │          │ Unauthorized   │
    ├─────────────┤          ├────────────────┤
    │ • Route     │          │ • User tries   │
    │   invalid   │          │   to access    │
    │ • File not  │          │   other        │
    │   found     │          │   user's data  │
    └─────────────┘          └────────────────┘
         │                       │
         └───────────┬───────────┘
                     │
                     ▼
        ┌──────────────────────────────────┐
        │ Show Error Page with            │
        │ • Status code                  │
        │ • Error message               │
        │ • Back button/home link       │
        │ • Suggestion/help text        │
        └──────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────┐
│            Production Environment                   │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Web Server (Nginx/Apache)                         │
│    └─ Reverse Proxy                                │
│                                                      │
│  Laravel Application                               │
│    ├─ app/                                         │
│    ├─ routes/                                      │
│    ├─ resources/                                   │
│    ├─ storage/                                     │
│    └─ bootstrap/                                   │
│                                                      │
│  PHP-FPM / HHVM                                    │
│    └─ Application Processing                       │
│                                                      │
│  MySQL Database                                    │
│    ├─ Data Storage                                 │
│    ├─ Indexing                                     │
│    └─ Backups                                      │
│                                                      │
│  Cache (Redis/Memcached)                          │
│    ├─ Sessions                                     │
│    ├─ Query Results                                │
│    └─ View Cache                                   │
│                                                      │
│  File System                                       │
│    ├─ storage/uploads                             │
│    ├─ storage/logs                                │
│    └─ storage/cache                               │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

**Architecture Documentation Complete!**  
*For implementation details, see other documentation files.*

